import Placeholder from '../components/Placeholder'
export default function Returns() {
  return (
    <Placeholder
      eyebrow="Service"
      title="Returns"
      desc="Warehouse or PAFC logs a return: search the order, pick where it came back to (AWDS Warehouse or Port Store Alberton), set the date, select the items, and record the reason."
      phase="Phase 5"
    />
  )
}

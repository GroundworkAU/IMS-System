import Placeholder from '../components/Placeholder'
export default function Products() {
  return (
    <Placeholder
      eyebrow="Purchasing"
      title="Products"
      desc="Every product with its brand, sizes, cost, retail and barcode. Keeps the supplier's own SKU and product name so indent reorders match even under a different display name."
      phase="Phase 1 — in progress"
      note="Barcodes can arrive later; the record supports none / partial / complete states."
    />
  )
}
